<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Registration Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        form {
            max-width: 400px;
            margin: auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        label {
            display: block;
            margin: 8px 0 4px;
        }
        input {
            width: 100%;
            padding: 8px;
            margin: 4px 0 12px;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h2>Edit Registration Details</h2>
    <form th:action="@{/update}" th:object="${registration}" method="post">
        <input type="hidden" name="id" th:field="*{id}" />

        <label for="name">Name:</label>
        <input type="text" name="name" th:field="*{name}" required />

        <label for="password">Password:</label>
        <input type="password" name="password" th:field="*{password}" required />

        <label for="mobilenumber">Contact Number:</label>
        <input type="number" name="mobilenumber" th:field="*{mobilenumber}" required />

        <label for="gender">Gender:</label>
        <input type="text" name="gender" th:field="*{gender}" required />

        <label for="state">State:</label>
        <input type="text" name="state" th:field="*{state}" required />

        <label for="qualification">Qualification:</label>
        <input type="text" name="qualification" th:field="*{qualification}" required />

        <label for="hobbies">Hobbies:</label>
        <input type="text" name="hobbies" th:field="*{hobbies}" />

        <label for="address">Address:</label>
        <input type="text" name="address" th:field="*{address}" required />

        <button type="submit">Save</button>
    </form>
</body>
</html>
